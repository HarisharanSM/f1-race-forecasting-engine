# Export these variables in your shell; no secrets are needed for the offline demo.
OPENAI_API_KEY=
# Choose a Responses API model supporting Structured Outputs.
F1_LLM_MODEL=
# Optional: use a fine-tuned model for the outcome head only.
F1_OUTCOME_MODEL=
F1_DATABASE=data/forecast.sqlite3
# Optional locally trained Transformer directory; no API key required for this model.
F1_ML_MODEL=artifacts/real-data/model
